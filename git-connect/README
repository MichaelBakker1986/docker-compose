#does not work
git init
git config core.sparseCheckout true
echo "*CODELISTS/*" >> .git/info/sparse-checkout
git remote add -f origin ssh://git@stash.topicus.nl:7999/ff/finanfinancials.git

Will be store for
 - FFL
 - Matrices
 - Translation files
 - Screen definition (if needed)