{
  "name": "lme-all",
  "private": true,
  "version": "2.0.0",
  "description": "lme-all",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^3.5.0",
    "jstat": "^1.7.1",
    "xhr2": "^0.1.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "test"
  ],
  "scripts": {
    "install": "concurrently \"cd log && npm install --no-optional \" \"cd lme-data-api && npm install --no-optional \" \"cd excel-connect && npm install --no-optional \" \"cd lme-core && npm install --no-optional \" \"cd lme-model-api && npm install --no-optional\" \"cd formulajs-connect && npm install --no-optional\" \"cd demo-apps && npm install --no-optional --allow-root\" \"cd git-connect && npm install --no-optional\" \"cd math && npm install --no-optional\" \"cd model-tests && npm install --no-optional\" \"cd proxy && npm install --no-optional\"",
    "test": "concurrently \"cd excel-connect && npm test\" \"cd lme-core && npm test\" \"cd model-tests && npm test\" \"cd lme-model-api && npm test --no-update-notifier\" \"cd log && npm test --no-update-notifier\"",
    "start": "concurrently \"cd lme-model-api && node update.js\""
  },
  "config": {
    "unsafe-perm": true
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@stash.topicus.nl:7999/ff/fesjs.git"
  },
  "transform": [["babelify", { "comments": false }]],
  "presets": [
    "env"
  ],
  "keywords": [
    "lme"
  ],
  "author": "michael.bakker",
  "license": "ISC"
}
