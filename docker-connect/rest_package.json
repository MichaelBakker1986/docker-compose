{
  "name": "lme-restapi",
  "private": true,
  "version": "2.0.0",
  "description": "lme-all",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^3.5.0"
  },
  "devDependencies": {
    "xhr2": "^0.1.4",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "test"
  ],
  "scripts": {
    "install": "concurrently \"cd log && npm install --no-optional \" \"cd lme-data-api && npm install --no-optional \" \"cd proxy && npm install --no-optional\" \"cd excel-connect && npm install --no-optional \" \"cd lme-core && npm install --no-optional\" \"cd formulajs-connect && npm install --no-optional\" \"cd git-connect && npm install --no-optional\" \"cd math && npm install --no-optional\"",
    "start": "NODE_ENV=production concurrently \"cd lme-data-api && node lme-data-app.js\" \"cd proxy && node proxy.js\""
  },
  "config": {
    "unsafe-perm": true
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@stash.topicus.nl:7999/ff/fesjs.git"
  },
  "keywords": [
    "lme"
  ],
  "author": "michael.bakker",
  "license": "ISC"
}
